import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'umss.ptaang.licorcito',
  appName: 'Licorcito',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
